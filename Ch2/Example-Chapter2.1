Ready> Parsed an extern.
Ready> Ready> Parsed a function definition.
Ready> Parsed a top-level expression.
Ready> $
proto sin(arg)
def proto add(x y)
(x + sin(y))
def proto __anon_expr()
add(3.000000, 7.000000)